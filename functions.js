"use strict";
const sum = (x, y) => {
    return (x + y).toString();
};
const value = sum(2, 3);
console.log(value);
//"Void" => função void não retorna nada
const log = (messege) => {
    console.log(messege);
};
const subtrair = (x, y) => {
    return x - y;
};
